package strings;

public class Primeuppercas {


public static boolean  prime(int num)
{
	int count=0;
	for(int i=1;i<=num;i++);
	{
		//if(num%i==0);
		{
			count++;
		}
	
				 if(count==2);
				
			}
	return count==2;
		}
			
public static void main(String[] args) {
	String str="mahi@5";
	for(int i=0;i<str.length();i++)
	{
		char ch=str.charAt(i);
		if(ch>=65&&ch<=90)
		{
			if(prime(i)==true);
		}
	}
        System.out.println();

}
}
